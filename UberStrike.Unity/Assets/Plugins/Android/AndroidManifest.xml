<?xml version="1.0" encoding="utf-8"?>
<!--
  Custom main Android manifest for the UberStrike mobile (IL2CPP) build.

  Reason this file exists:
    ApplicationDataManager's #if UNITY_ANDROID config block points ContentBaseUrl at
    http://client-dev.uberforever.eu/ (maps / images / audio are served over plain
    HTTP — the host has no HTTPS listener). Android 9+ (API 28+) blocks cleartext
    traffic by default, so without android:usesCleartextTraffic="true" the lobby
    content silently fails to download even though https auth to ws-dev succeeds.

  Unity's gradle manifest-merger injects uses-sdk, icons, label and any
  build-required entries on top of this file (useCustomMainManifest = 1).
-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application android:usesCleartextTraffic="true">
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  android:theme="@style/UnityThemeSelector"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>
    </application>
</manifest>
